const navIcon = document.querySelector("nav-icon img");

const minWidth = window.matchMedia("(min-width: 560px)");

window.addEventListener("resize", () => {
  if (minWidth.matches) {
  }
});
